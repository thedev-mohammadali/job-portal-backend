enum UserRole {
    JOB_SEEKER
    EMPLOYER
    ADMIN
    SUPER_ADMIN
}

enum JobType {
    FULL_TIME
    PART_TIME
    CONTRACT
    INTERNSHIP
    FREELANCE
}

enum WorkMode {
    ONSITE
    REMOTE
    HYBRID
}

enum ExperienceLevel {
    ENTRY
    MID
    SENIOR
    LEAD
}

enum JobStatus {
    DRAFT
    PUBLISHED
    CLOSED
    ARCHIVED
}

enum ApplicationStatus {
    SUBMITTED
    UNDER_REVIEW
    SHORTLISTED
    INTERVIEW_SCHEDULED
    SELECTED
    REJECTED
    WITHDRAWN
}
